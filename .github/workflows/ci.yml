name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.22.x"
      - run: go test ./...
      - run: go vet ./...
      - run: go build -o bin/agentproof ./cmd/agentproof

  windows-record-lock:
    if: github.event_name == 'pull_request'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.22.x"
      - shell: pwsh
        run: |
          if ((git rev-parse HEAD).Trim() -ne "${{ github.event.pull_request.head.sha }}") {
            throw "checkout did not resolve the pull request head SHA"
          }
      - run: go test ./internal/record

  verify:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.22.x"
      # A test failure must reach AgentProof as failed evidence, not abort the job.
      - run: mkdir -p .agentproof/inputs && (go test -json ./... > .agentproof/inputs/go-test.jsonl || true)
      - uses: ./
        with:
          base: origin/${{ github.base_ref }}
          test-results: .agentproof/inputs/go-test.jsonl
          require-tests: "true"
          fail-on: critical
      - name: Report failed test events
        if: failure()
        run: jq -c 'select(.Action == "fail") | {Action, Package, Test, Elapsed}' .agentproof/inputs/go-test.jsonl
